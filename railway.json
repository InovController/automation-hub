{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && (ls /app/apps/api/dist/src/main.js && echo 'main.js OK' || echo 'main.js NOT FOUND') && node /app/apps/api/dist/src/main",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
