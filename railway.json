{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node /app/apps/api/dist/src/main",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
