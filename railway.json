{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'npx prisma migrate deploy && exec node /app/apps/api/dist/src/main'",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
